import { describe, it, expect } from 'vitest';
import { Statistics } from './statistics';

describe('Statistics', () => {
  it('should create', () => {
    expect(Statistics).toBeDefined();
  });
});
